import pg from "pg";

const HomePage = () => {
  // create connection to db
  // make query to db to get top athlete SELECT
  const topAthlete = // format data from db

  return <h1>{topAthlete}</h1>
}